#include <bits/stdc++.h>
#define int long long
#define PB emplace_back
#define endl "\n"
const int maxN = 5005;
using namespace std;

int n, ans;
vector<int> v;
int a[maxN],b[maxN];
signed main()
{
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    cin >> n;
    for (int i = 0; i < n;i++)
        cin >> a[i];
    for (int i = 0; i < n;i++)
        cin >> b[i];

}