#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ll t, n, k;
    cin >> t;
    while (t--)
    {
        cin >> n >> k;
        ll x[n],y[n];
        for (ll i = 0; i < n; i++)
        {
            cin >> x[i] >> y[i];
        }
    }

    return 0;
}