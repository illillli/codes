#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
    int t;
    cin >> t;
    while(t--){
        ll n;
        cin >> n;
        for (int i = 0; i < n;i++){
            cin >> input[i];
        }
        sort(input, input + n);
        
    }
}